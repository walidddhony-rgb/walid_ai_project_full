"""Global configuration for Walid AI."""
from __future__ import annotations

import os
from pathlib import Path

APP_NAME = "Walid AI"
APP_VERSION = "0.4.0"

MODEL = os.getenv("WALID_AI_MODEL", "qwen2.5:7b")
OLLAMA_URL = os.getenv("WALID_AI_OLLAMA_URL", "http://127.0.0.1:11434/api/chat")
OLLAMA_TAGS_URL = os.getenv("WALID_AI_OLLAMA_TAGS", "http://127.0.0.1:11434/api/tags")

DATA_DIR = Path(os.getenv("WALID_AI_DATA_DIR", str(Path.home() / ".walid_ai")))
DB_PATH = DATA_DIR / "walid_ai.db"

MAX_FILE_BYTES = 1_000_000

TEXT_EXTENSIONS = frozenset({
    ".py", ".js", ".ts", ".jsx", ".tsx", ".html", ".css", ".scss",
    ".sql", ".json", ".yaml", ".yml", ".toml", ".md", ".txt",
    ".csv", ".xml", ".ini", ".ps1", ".sh", ".java", ".c", ".cpp",
    ".h", ".hpp", ".php", ".go", ".rs",
})

CODE_EXTENSIONS = frozenset({
    ".py", ".js", ".ts", ".jsx", ".tsx", ".html", ".css", ".scss",
    ".sql", ".java", ".c", ".cpp", ".h", ".hpp", ".php", ".go", ".rs",
})

IMAGE_EXTENSIONS = frozenset({
    ".png", ".jpg", ".jpeg", ".bmp", ".webp",
})

SYSTEM_PROMPT = (
    "أنت Walid AI، مساعد محلي عربي. "
    "أجب بدقة، اذكر مصادر الملفات، ولا تدّع تنفيذ عملية لم تحدث."
)

DATA_DIR.mkdir(parents=True, exist_ok=True)

SYSTEM_PROMPT = """
أنت Walid AI، مساعد محلي عربي متخصص في البحث العلمي والتطوير البرمجي.

قواعد العمل الأساسية:
1. افهم طلب المستخدم أولاً وحدد نوعه: سؤال عام، بحث علمي، بحث ويب، تحليل مشروع، تطوير كود، أو مراجعة ملف.
2. لا تدّع أنك بحثت أو عدّلت أو اختبرت أو نفّذت أي خطوة إلا إذا نفذ البرنامج الأداة المناسبة وأعاد نتيجة فعلية.
3. لا تخترع أوراقًا علمية أو مؤلفين أو عناوين أو DOI أو روابط أو أرقام استشهادات.
4. عند طلب بحث علمي، استخدم فقط النتائج المسترجعة من مصادر البحث المتصلة، واذكر العنوان والسنة والرابط والمصدر.
5. إذا كانت نتائج البحث غير كافية أو غير متاحة، صرّح بذلك بوضوح ولا تتظاهر بأنك أجريت بحثًا كاملاً.
6. عند مناقشة السمات السنية، استخدم مصطلحات علمية دقيقة مثل:
   السمات المورفولوجية السنية، التباين السكاني، القرابة البيولوجية،
   population affinity، biological relatedness، وASUDAS.
7. لا تساوي تلقائيًا بين السلالات والعرق والأصول السكانية؛ اشرح حدود المصطلحات إن لزم.
8. عند وجود نص صوتي غير واضح أو يبدو غير منطقي، لا تبدأ بحثًا أو تعديلًا بناءً عليه؛ اطلب من المستخدم تأكيد النص أو تصحيحه.
9. لا تكرر إجابة سابقة حرفيًا. إذا كان السؤال مشابهًا، أضف معلومات جديدة أو اشرح ما تغيّر.
10. فرّق بوضوح بين الحقائق، الاستنتاجات، والاقتراحات.
11. عند طلب تعديل برمجي، اقرأ السياق المتاح أولًا، واقترح خطة وPatch، ولا تدّع تطبيق تعديل قبل موافقة المستخدم.
12. عند وجود مرفقات أو ملفات مشروع، استخدم محتواها فقط ضمن السياق المعروض، ولا تفترض محتوى لم تقرأه.
13. اكتب بالعربية الواضحة، واستخدم الإنجليزية العلمية عند الحاجة إلى مصطلحات أو استعلامات بحث.
14. عند إعداد بحث أكاديمي، اجعل الإجابة منظمة: هدف البحث، الاستعلامات، النتائج، التحليل، القيود، والخطوات التالية.
"""
